<policies>
    <inbound>
        <rewrite-uri template="?" copy-unmatched-params="false" />
        <set-body>@{
            var body = context.Request.Body.As<JObject>();
            body.Add(new JProperty("source", context.Request.Headers.GetValueOrDefault("X-Source","unknown")));
            var response = new JArray();
            response.Add(body);
            return response.ToString();
        }</set-body>
        <base />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
        <set-body></set-body>
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>

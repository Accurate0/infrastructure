apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: owntracks-recorder-config
  name: owntracks-recorder-config
spec:
  storageClassName: longhorn-prod
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

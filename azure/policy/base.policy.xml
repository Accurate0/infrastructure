<policies>
    <inbound>
        <cors allow-credentials="true">
            <allowed-origins>
                <origin>http://localhost:3000/</origin>
                <origin>https://isitww3yet.com/</origin>
                <origin>https://maccas.anurag.sh/</origin>
            </allowed-origins>
            <allowed-methods preflight-result-max-age="300">
                <method>GET</method>
                <method>POST</method>
                <method>DELETE</method>
                <method>PATCH</method>
            </allowed-methods>
            <allowed-headers>
                <header>*</header>
            </allowed-headers>
            <expose-headers>
                <header>*</header>
            </expose-headers>
        </cors>
        <!-- always delete api key headers... no one needs these -->
        <set-header name="X-Api-Key" exists-action="delete" />
        <set-header name="Ocp-Apim-Subscription-Key" exists-action="delete" />
    </inbound>
    <backend>
        <forward-request />
    </backend>
    <outbound>
        <set-header name="X-Api-Key" exists-action="delete" />
        <set-header name="Ocp-Apim-Subscription-Key" exists-action="delete" />
        <set-header name="Strict-Transport-Security" exists-action="override">
            <value>max-age=31536000</value>
        </set-header>
        <set-header name="X-XSS-Protection" exists-action="override">
            <value>1; mode=block</value>
        </set-header>
        <set-header name="Content-Security-Policy" exists-action="override">
            <value>script-src 'self'</value>
        </set-header>
        <set-header name="X-Frame-Options" exists-action="override">
            <value>deny</value>
        </set-header>
        <set-header name="X-Content-Type-Options" exists-action="override">
            <value>nosniff</value>
        </set-header>
        <set-header name="Expect-Ct" exists-action="override">
            <value>max-age=604800,enforce</value>
        </set-header>
        <set-header name="Cache-Control" exists-action="skip">
            <value>max-age=900, must-revalidate</value>
        </set-header>
        <!-- general server headers -->
        <set-header name="X-Powered-By" exists-action="delete" />
        <set-header name="Server" exists-action="delete" />
        <!-- drop CF headers -->
        <set-header name="Report-To" exists-action="delete" />
        <set-header name="CF-Cache-Status" exists-action="delete" />
        <set-header name="CF-RAY" exists-action="delete" />
        <set-header name="NEL" exists-action="delete" />
        <!-- AWS headers -->
        <set-header name="x-amz-apigw-id" exists-action="delete"/>
        <set-header name="x-amzn-RequestId" exists-action="delete"/>
        <set-header name="X-Amzn-Trace-Id" exists-action="delete"/>
    </outbound>
    <on-error />
</policies>

<policies>
    <inbound>
        <base />
        <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized. Access token is missing or invalid.">
            <openid-config url="https://apib2clogin.b2clogin.com/login.anurag.sh/B2C_1_SignIn/v2.0/.well-known/openid-configuration" />
            <audiences>
                <audience>f285a3b9-c589-4fae-971e-edd635df6b96</audience>
            </audiences>
        </validate-jwt>

        <set-variable name="userid" value="@{
                var authHeader = context.Request.Headers.GetValueOrDefault("Authorization", "");
                return authHeader.AsJwt()?.Claims.GetValueOrDefault("oid", "");
        }"/>
        <choose>
            <when condition="@(context.Variables.GetValueOrDefault("userid", "") == "")">
                <return-response>
                    <set-status code="404" reason="Not Found"/>
                 </return-response>
            </when>
            <otherwise>
                <set-header name="X-Api-Key" exists-action="override">
                    <value>{{MaccasAPIMPolicy}}</value>
                </set-header>
                <rewrite-uri template="?" />
                <set-backend-service base-url="@{
                    return "https://api.anurag.sh/kvp/v1/MACCAS_WEB_" + context.Variables.GetValueOrDefault("userid", "");
                }" />
                </otherwise>
        </choose>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
